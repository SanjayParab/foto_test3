<%= form_for(@phot, :html => { :multipart => true }) do |f| %>
  <% if @phot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phot.errors.count, "error") %> prohibited this phot from being saved:</h2>

      <ul>
      <% @phot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <br><%= f.label :image %><br/>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
